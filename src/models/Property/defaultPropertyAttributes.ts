import PropertyAttributes from './PropertyAttributes';

const defaultPropertyAttributes: PropertyAttributes = {
  name: 'Homie Property',
  settable: false,
  retained: true,
  unit: '',
  datatype: 'string',
  format: '',
};

export default defaultPropertyAttributes;
